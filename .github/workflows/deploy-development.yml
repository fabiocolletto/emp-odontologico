name: Deploy Pages | development

on:
  push:
    branches:
      - development
  workflow_dispatch:

jobs:
  deploy:
    permissions:
      contents: write
    uses: ./.github/workflows/deploy-shared.yml
    with:
      target_env: development
